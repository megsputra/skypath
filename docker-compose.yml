
services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./flights.json:/data/flights.json:ro
    environment:
      - FLIGHTS_JSON_PATH=/data/flights.json

  frontend:
    build:
      context: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
